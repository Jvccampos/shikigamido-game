services:
  game:
    build: .
    ports: ["3000:3000"]
    environment:
      PUBLIC_URL: ${PUBLIC_URL:-http://localhost:3000}
      NODE_ENV: ${NODE_ENV:-development}
    volumes: ["game-data:/data"]
    restart: unless-stopped
volumes:
  game-data:
